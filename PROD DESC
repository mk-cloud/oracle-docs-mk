SELECT 
    SUBSTR(product_description, 56, 1) as char_at_56,
    ASCII(SUBSTR(product_description, 56, 1)) as ascii_code,
    DUMP(SUBSTR(product_description, 56, 1)) as hex_dump
FROM your_table
WHERE product_description LIKE '%EntMobandSecE3%';
